<Window x:Class="miniDSClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:miniDSClient"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Левая панель -->
        <Grid Grid.Column="0" Background="#2f3136">
            <StackPanel>
                <Button x:Name="CreateChannelButton" Content="+ Создать канал" Margin="8" Click="CreateChannelButton_Click"/>
                <ListBox x:Name="ChannelsList" Background="Transparent" BorderThickness="0" SelectionChanged="ChannelsList_SelectionChanged" HorizontalContentAlignment="Stretch">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Grid.Column="0"/>
                                <Button Content="+" Background="Transparent" Click="AddUserButton_Click" 
                    Grid.Column="1" Width="15" BorderThickness="0" Foreground="Green"/>
                                <Button Content="x" Background="Transparent" Click="DeleteChannelButton_Click" 
                    Foreground="Red" Grid.Column="2" Width="15" BorderThickness="0"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Grid>

        <!-- Правая панель -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <ListBox x:Name="MessagesList" Grid.Row="0"/>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="70"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="MessageInput" Grid.Column="0" KeyDown="MessageInput_KeyDown"/>
                <Button x:Name="SendButton" Grid.Column="1" Click="SendButton_Click" Content="Отправить"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
